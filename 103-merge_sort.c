#include "sort.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * merge_sort - actual merge sorting
 * @array: whats being sorted
 * @size: size of array
 */
void merge_sort(int *array, size_t size)
{
	int *temp;
	free(temp);
}
